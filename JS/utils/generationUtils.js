//Funcon para actualizar el total de Ids (suma de a uno)
function generarID(tipo, totalId) {
	if (tipo === "ingreso") {
		totalId += 1;
		return totalId;
	} else {
		totalId += 1;
		return totalId;
	}
}
